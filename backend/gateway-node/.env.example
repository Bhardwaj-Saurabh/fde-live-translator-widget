# Node gateway config — copy to `.env`
PORT=8787
AI_SERVICE_URL=http://localhost:8000

# Per-IP rate limit on /translate + /translate/batch (0 disables).
# 300/min never trips bench.py (~80 req/run) or normal widget use.
RATE_LIMIT_MAX=300
RATE_LIMIT_WINDOW_SEC=60
